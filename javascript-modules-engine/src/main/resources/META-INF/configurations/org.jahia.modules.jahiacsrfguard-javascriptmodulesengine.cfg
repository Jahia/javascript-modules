# Whitelists the single generic JS action dispatch endpoint (see GenericActionEndpoint and
# ADR-0008). The endpoint is protected against classic CSRF by a mandatory custom header
# (X-JS-Action) that HTML forms cannot set and that cross-origin scripts cannot send without a
# CORS preflight.
whitelist = *.jsAction.do
