JAHIA_VERSION=${JAHIA_VERSION:-8.2-SNAPSHOT}
JAHIA_IMAGE=${JAHIA_IMAGE:-jahia/jahia-ee-dev:${JAHIA_VERSION}}
TESTS_IMAGE=${TESTS_IMAGE:-jahia/javascript-modules-engine:latest}
MANIFEST=${MANIFEST:-provisioning-manifest-build.yml}
JAHIA_URL=${JAHIA_URL:-http://jahia:8080}
SUPER_USER_PASSWORD=${SUPER_USER_PASSWORD:-root1234}
NEXUS_USERNAME=${NEXUS_USERNAME:-NEXUS_USERNAME}
NEXUS_PASSWORD=${NEXUS_PASSWORD:-NEXUS_PASSWORD}
CYPRESS_PREPACKAGED_SITE_URL="jar:mvn:org.jahia.samples/javascript-modules-samples-hydrogen-prepackaged/LATEST/zip!/site.zip"
