{
  // The build excludes the specs (they must not reach `dist`), so they need their own pass to be
  // type-checked at all.
  "extends": "./tsconfig.json",
  "compilerOptions": { "noEmit": true },
  "exclude": ["node_modules"],
  "include": ["src/**/*.ts", "src/**/*.tsx"]
}
